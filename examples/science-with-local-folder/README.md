# Science With Local Folder

This example documents the ordinary science workflow shape where a Desktop
project points at a user workspace folder on the remote server.

Use `examples/science-minimal/experiment.yaml` as the minimal executable Core
Backend config and adapt the task workspace path to the remote folder prepared
by Desktop.
