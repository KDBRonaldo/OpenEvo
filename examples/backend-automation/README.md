# Backend Automation

This directory is for maintainer automation around `openevo-backend`, release
smokes, and remote backend checks. These examples are not ordinary-user Desktop
quickstarts.

Start with:

```bash
openevo-backend run examples/science-minimal/experiment.yaml --dry-run --json
```
